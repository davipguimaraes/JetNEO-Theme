@model DomainProduct.Result

@if (Model.Products != null && Model.Products.Any())
{
    <div class="ui container topsellers">

        <div class="ui horizontal divider">
            Em Destaque
        </div>
        <!-- Page: ~/Views/Shared/Blocks/Product/_ProductListSlider.cshtml -->
        @Html.Partial("~/Views/Shared/Blocks/Product/_ProductListSlider.cshtml", Model.Products)

    </div>
}