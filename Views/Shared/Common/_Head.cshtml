@{
    string _classSearch = Session["idEventList"] == null ? "search" : "filterListEventsProducts";
}
<div class="ui container fluid _header">
    <div class="ui container">
        <div class="ui middle aligned three column centered grid">
            <div class="row">
                <div class="ui ten wide mobile six wide tablet four wide computer column">
                    <a href="@(Session["idEventList"] == null ? "/Home" : "#")">
                        <img class="ui middle aligned image" src="/assets/image/logo/logo.png" alt="">
                    </a>
                </div>
                <div class="ui center aligned five wide tablet eight wide computer column searchcolumn">
                    <div class="ui category @_classSearch">
                        <div class="ui icon input fluid">
                            <input class="prompt" type="text" placeholder="Buscar...">
                            <i class="search icon"></i>
                        </div>
                        <div class="results"></div>
                    </div>
                </div>
                <div class="ui right aligned six wide mobile five wide tablet four wide computer column">
                    <div id="cartButton" class="cartbutton mini-cart" tabindex="0" data-segment="@(Session["segment"] != null ? Session["segment"] : "b2c")">
                        <div class=" ">
                            <span>@(Session["idEventList"] == null && Session["EventList"] == null ? "Meu Carrinho" : "Minha Lista")</span>
                            <i class="cart icon huge"></i>
                        </div>
                        <a class="mini-cart-count" id="total_itens_card">
                            @{
                                if (Session["EventList"] == null)
                                {
                                    @Html.Action("TotalItensCart", "Checkout")
                                }else
	                            {
	                                @Html.Action("TotalItensList", "EventList")
                                }
                            }
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>